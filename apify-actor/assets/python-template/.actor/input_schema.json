{
    "title": "Input schema for My Actor",
    "description": "Configure the actor behavior and specify what to process.",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "startUrl": {
            "sectionCaption": "Input URL",
            "title": "Start URL",
            "type": "string",
            "description": "The URL where the actor will start.",
            "editor": "textfield",
            "pattern": "^https?://[^\\s/$.?#].[^\\s]*$",
            "prefill": "https://example.com"
        },
        "maxItems": {
            "sectionCaption": "Limits",
            "title": "Maximum items",
            "type": "integer",
            "description": "Maximum number of items to process.",
            "editor": "number",
            "minimum": 1,
            "maximum": 1000,
            "default": 10
        }
    },
    "required": ["startUrl"]
}
